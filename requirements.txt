# Dependencias de CodeXHound

# Web Backend
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Inteligencia Artificial
openai>=1.12.0          # Usado con Groq (compatible con OpenAI SDK) y OpenAI
google-genai>=0.1.0     # Google Gemini
groq>=0.9.0             # Cliente oficial Groq (fallback nativo)
requests>=2.31.0        # Peticiones HTTP genéricas

# Analizador Estático (AST)
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0
tree-sitter-php>=0.21.0
tree-sitter-javascript>=0.21.0

# Nota: El selector de carpetas nativo utiliza 'tkinter' (incluido en la biblioteca estándar de Python).
# En sistemas Linux (Ubuntu/Debian) sin interfaz GUI instalada por defecto, puede ser necesario instalarlo con:
#   $ sudo apt-get install python3-tk
